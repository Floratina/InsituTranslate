# InsituTranslate 协作规则

## 项目与命令

Tauri 2 桌面端多格式文档翻译工具。
- **技术栈**：前端 React、Vite、TypeScript、Tailwind CSS、shadcn/ui、`motion`；后端 Rust、SQLite；包管理器 `pnpm`。
- **常用命令**：
  - 开发环境：`pnpm tauri dev`
  - 生产构建：`pnpm tauri build`
  - 前端类型检查与编译：`pnpm build`

## 编码前先思考
**不要假设。不要隐瞒困惑。摆明权衡。**
- 在实现之前：
  - 将需求转化为 2-5 个具体、可验证的验收标准。
  - 明确说明你的假设。如果不确定，请提问。
  - 如果存在多种理解，请将它们列出——不要默默做出选择。
  - 如果存在更简单的方法，请直说。在有必要时提出反对意见。
  - 如果有不明确的地方，停下来。指出令人困惑之处。提问。

### 核心工作流与格式规范
工作流：复制源文件，只替换可翻译内容，严格保留原始格式、排版、样式、布局、元数据与非文本资源。

- **PDF**：通过 MinerU CLI 或 API 转为 Markdown，翻译后重建文档结构。
- **Markdown (`.md`)** ：翻译文本，完整保留标题、列表、强调、代码块、链接等语法。
- **HTML (`.html`/`.htm`)** ：仅翻译可见文本节点，严格保留标签和属性。
- **EPUB (`.epub`)** ：解包，就地翻译内部 HTML/XHTML，再重新打包。
- **JSON (`.json`)** ：仅翻译字符串值（String Values），保留键（Keys）和数据结构。
- **TXT (`.txt`)** ：直接翻译纯文本。
- **SRT (`.srt`)** ：严格保留字幕索引和时间轴（如 `00:01:20,000 --> 00:01:23,000`）。
- **ASS (`.ass`)** ：严格保留样式头（Style Headers）、时间块和事件属性。
- **LRC (`.lrc`)** ：严格保留时间标签（如 `[00:12.34]`）。
- **DOCX / XLSX（读写分离）** ：原 ZIP/XML 是回写的唯一真实依据；成熟 crate 仅负责只读校验，严禁通过只读 crate 重建文件。
	- **DOCX**：用 `docx-rs` 读取校验；仅修改 `word/document.xml` 目标文本节点，保留 run、样式、关系、媒体和未知 XML。
	- **XLSX**：用 `calamine` 读取校验；v1 仅修改 `xl/sharedStrings.xml`，严禁改动 worksheet XML，以保全公式、工作表、列宽和布局。

## 架构与实施原则

**扩展系统前先了解系统。保留既有的保障机制。**
在创建或扩展项目时：
- **上下文优先**：先读需求、相关代码和调用点；复用现有模式；只改完成当前需求所需的部分。
- **模块与职责拆分**：
	- `App.tsx` 仅保留应用壳、顶级导航和全局 Provider。
	- 页面级视图放置于 `src/views/`，通用与业务组件下沉至 `src/components/`；严禁堆积业务，也不得按文件行数机械拆分。
- **组件复用与防分化**：
	- 优先复用 `src/components/ui/` 和现有组件；修改共享组件前检查全部调用点。
	- 全局意图才做全局修改；局部差异使用已有 props 或组合，严禁新建 `v2` / `Button-v2` 等冗余副本组件。
- **特性内聚与就近封装**：保持功能逻辑的模块化，但要通过所采用架构和框架的生命周期及控制边界来进行集成，例如依赖注入、路由、状态管理、持久化、中间件和配置。
- **切勿通过绕过这些边界来实现功能**，从而导致框架钩子、保障机制、横切关注点行为或现有功能失效。
- **反过度工程与防御性编程**：
	- 严禁为泛滥的假设需求增加抽象层、状态、兼容层、缓存或重试机制。
	- 不针对不可能发生的情况进行错误处理。
	- 具体反馈默认仅约束当前所述问题。除非 Floratina 明确要求普遍规则，不追加全局排除逻辑或多余状态。
	- 修复根本原因，不围绕单次现象建立长期禁令；新增复杂度必须对应当前需求或可复现缺陷。
- **类型安全**：TypeScript 必须声明明确类型，严格避免 `any`；Rust 保持既有模块和 Result/Error 错误边界。

## 错误、数据与安全边界

- **零前端直接请求（硬性安全红线）** ：
	- 前端 React 严禁直接发起任何外部网络请求（禁止在前端使用 `fetch`、`axios` 等，无论目标接口是否免费或无需凭证）。
	- 所有外部网络交互、翻译服务与 MinerU 均必须通过 Tauri IPC 由 Rust 后端安全执行。
	- API 密钥、JWT 等凭证严禁硬编码或存入前端状态/代码。
- **契约校验与防吞错**：
	- 禁止 catch-all、吞错、返回空字符串、默认伪成功或静默降级。错误必须在对应边界捕获并携带上下文明确返回或展示。
	- 仅在输入、IPC、文件、数据库等信任边界按契约校验；内部不重复校验；除非契约明确要求，不得随意 trim、过滤、纠正或丢弃原始数据。
- **只读参考目录**：`D:\AppData\0_repo_references\` 及其子目录严格只读；禁止创建、写入、修改、删除，亦不得作为 `path` 依赖。

## 前端与 UI / 动效规范

- **组件与图标**：优先复用 shadcn/ui，图标统一使用 `lucide-react`。
- **全局圆角（强制 6px）** ：所有组件、卡片、输入框、浮层和按钮，强制使用严格的 `rounded-[6px]`。
- **字号与相对单位**：统一使用 Tailwind 相对字号变量（`text-xs`, `text-sm`, `text-base` 等）；**严禁硬编码绝对像素字号**，确保根节点 font-size 缩放完全生效。
- **颜色与双色模式**：复用全局 CSS 变量（`bg-background`, `text-foreground` 等）；新增颜色 token 必须同时定义 light 与 dark 双色适配。
- **高密度紧凑布局**：
	- 减少无意义的 div 容器嵌套；减少卡片视图；保持紧凑间距（常用 `gap-2`, `gap-3`, `p-2`, `p-3`）。
	- 父级布局优先使用 Flexbox/Grid 的 `gap` 控制间距，严禁在子元素上随意累加 margin/padding 导致间距失控。
- **动效与过渡规范**：
	- **依赖库**：动画统一使用已有的 `motion`，从 `motion/react` 导入（严禁安装已废弃的 `framer-motion`）。
	- **参数统一**：复杂结构动效和弹簧效果统一复用 `src/lib/motion.ts`（消费 `APP_MOTION_EASE` / `APP_SPRING`）；简单悬浮、点击、淡入淡出优先用 Tailwind CSS 类（如 `transition-all duration-150 ease-out`）。
	- **时长红线（<=300ms）** ：微交互 100~150ms；下拉菜单/气泡浮层 150~200ms；折叠/弹窗 200~300ms。严禁编写超过 300ms 的迟缓过渡。
	- **组件自包含**：动画变体（Variants）与过渡配置须封装在组件文件内部，保持积木式解耦。
	- **交互稳定性**：按钮按下仅允许轻微缩放或高亮反馈，**严禁位移动画**（按钮内的文字和图标绝对禁止产生位移）。
- **滚动条与视口规范**：
	- 通用滚动区域必须复用 `src/components/ui/scroll-area.tsx` 中的 `ScrollArea`（横向/双轴使用 `axis="horizontal"` 或 `axis="both"`）。
	- 原生输入（如 Textarea）内部滚动使用 `scrollbar-native` 类；受第三方控件契约约束的浮层（如 Radix/shadcn `Select`）自绘滑块必须复用共享滚动条 Token，严禁硬编码独立样式。
	- 禁止全局 `preventDefault()` 或手动模拟滚轮；浮层滚动必须保证命中真实 viewport 并添加 `overscroll-contain` 防止背景穿透。

## 测试规范

- 常规改动只运行最相关的检查；仅在行为改变或存在真实回归风险时新增测试。优先使用表驱动用例与共享 fixture；不测试内部实现细节，不针对未提出的假设编写排除测试。
- **重构纪律**：在 Floratina 明确要求「重写/重构测试体系」前，严禁擅自迁移或重排现有测试。
- **体系要求（仅在明确触发后）** ：建立分层 Rust 测试矩阵。根层评估整套软件的关键工作流与子系统协同；各子目录采用集中 `tests.rs` 评估本子系统能力与可用状态；共享 fixture 并合并散落重复的用例。
- **消融实验**：每次写完代码，进行消融实验，证明你新添加的模块、特征或设计不是“凑数”的，而是真正起到了提升性能的作用。
